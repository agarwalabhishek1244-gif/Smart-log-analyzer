# Smart-log-analyzer
I developed a Python-based log analyzer to process and analyze system log data using the Pandas library. The project reads structured log data from a CSV file and performs operations such as filtering, grouping, and counting log levels to identify error patterns and system behavior.
